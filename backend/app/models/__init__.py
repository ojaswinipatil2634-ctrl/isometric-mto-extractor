# app/models
#
# Reserved for ORM / persistence models if this service is later backed by
# a database (e.g. to store historical extractions). Currently the app is
# stateless per-request, so all data contracts live in app/schemas instead.
